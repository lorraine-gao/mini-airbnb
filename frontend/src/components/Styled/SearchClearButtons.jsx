import { styled } from '@mui/material/styles';
import Box from '@mui/material/Box';

const SearchClearButtons = styled(Box)({
  flexBasis: '100%',
  display: 'flex',
  justifyContent: 'space-around',
  gap: '5px',
  flexWrap: 'wrap',
});

export default SearchClearButtons;
